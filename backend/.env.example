DB_HOST=
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=

# Comma-separated list of allowed origins.
# Example for prod: https://your-app.vercel.app,http://localhost:3000
CORS_ORIGINS=http://localhost:3000

# Optional: regex to match Vercel preview URLs (uncomment to use).
# CORS_ORIGIN_REGEX=^https://your-app-[a-z0-9-]+\.vercel\.app$
